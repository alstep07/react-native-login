import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    alignSelf: 'stretch',
  },
  subtitle: {
    marginTop: 30,
    color: '#fff',
    fontSize: 14,
    lineHeight: 16,
    textAlign: 'center',
  },
  title: {
    marginTop: 8,
    fontSize: 24,
    color: '#fff',
    textAlign: 'center',
  },
});

export default styles;
