import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  gradient: {
    borderRadius: 10,
  },
  button: {
    width: '100%',
    paddingVertical: 22,
    borderRadius: 10,
  },
  title: {
    textAlign: 'center',
    fontWeight: '700',
    fontSize: 18,
    lineHeight: 20,
    color: '#fff',
  },
});

export default styles;
